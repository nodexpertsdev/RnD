const header = {
  authkey: 'successive',
};

const route = {
  product: '/api/products',
  user: '/api/user',
};

const status = {
  internalServerError: 500,
  ok: 200,
};

export default {
  header,
  route,
  status,
};
