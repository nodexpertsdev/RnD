const header = {
  authkey: 'successive',
};

const route = {
  order: '/api/order',
  user: '/api/user',
};

const status = {
  internalServerError: 500,
  ok: 200,
};

export default {
  header,
  route,
  status,
};
